# TODO

Active queue. Completed product-facing changes move to `CHANGELOG.md`.
Current oracle scores live in `compatibility/candidate-status.md`.

## Current state

- Cloudflare production infrastructure now exists under the plain `zotero`
  names:
  - Worker: `zotero`
  - Custom domain: `zotero.peacockery.studio`
  - D1 database: `zotero`
  - R2 bucket: `zotero-attachments`
- The deployed Worker boots with D1/R2 bindings and `/health` returns
  `{"ok":true,"service":"zotero"}` at the Cloudflare edge.
- Routine deploys are durable from this machine through the ignored
  `server/.env` `CLOUDFLARE_API_TOKEN`; local Wrangler OAuth is no longer on the
  critical path.
- Latest deployed Worker: `410d4f5e-b8c9-472c-9216-6fe9e938f922` on
  `zotero.peacockery.studio`.
- The full live official v3 suite against the deployed Cloudflare D1/R2 path
  is green for every non-pending test: `451 passing`, `22 pending`, `0 failing`
  on 2026-07-09T09:03:43Z. The pending cases are upstream-skipped `schema` and
  `tts` cases; there are no known failing official v3 API slices in the latest
  live run.
- A real `/Applications/Zotero.app` smoke using a disposable profile passed
  against the live Worker on 2026-07-09T09:04:06Z: create book, create child
  note, add stored-file attachment, sync data/files, edit the book, trash the
  note, and sync again.
- The same desktop smoke is repeatable with `cd server && bun run smoke:desktop`
  and verifies the remote title, trash, attachment ZIP, and full-text response.
- Server tooling is on TypeScript `7.0.2`, Wrangler `4.110.0`, Biome `2.5.2`,
  and `worker-configuration.d.ts` generated by `wrangler types`. `bun run
  typecheck` regenerates Cloudflare Worker types before `tsc --noEmit`.
- Package dependencies are current as of 2026-07-09. Biome is intentionally
  pinned at `2.5.2` because `2.5.3` panicked in this project during module
  resolution.
- `server/src` is organized into `domain/`, `lib/`, `types/`, and `routes/`.
  Compatibility route helpers live under `routes/compatibility/support/`
  instead of a root-level route grab bag.
- Product docs are collapsed to the server README, Cloudflare production
  runbook, compact project-shape note, and compatibility maintenance/status
  files. Local third-party `references/` checkouts are ignored and reproduced
  from the tracked upstream oracle lock.
- `bun run check` now runs inside Cloudflare's Workers Vitest integration with
  isolated D1/R2 bindings and all tracked migrations. The runtime suite covers
  D1 persistence/version preconditions, an R2 attachment round trip, and real
  bsdiff/xdelta/vcdiff fixtures through the bundled compiled WASM modules.
- The upstream Zotero oracle is pinned by exact commit and schema digest;
  `compat:setup`, `compat:status`, and `compat:update` manage the ignored
  checkout. The pinned local `general,version` slice is `30/30` against
  Wrangler D1/R2.
- The complete pinned local suite after the runtime refactor is `445 passing`,
  `22 pending`, and `6` external-infrastructure failures from direct upstream
  S3/DynamoDB/HTTPS assertions. The live Cloudflare baseline remains `451
  passing`, `22 pending`, `0 failing`.
- The real desktop smoke also exercises Zotero Desktop full-text upload. It
  previously exposed and the server now fixes the batch full-text response shape
  expected by desktop (`successful[index].key`) and the full-text 412 version
  header.
- The live `file` behavior is covered by the full official run, including
  R2-backed form uploads, old-style `md5/filename` storage URLs, client v5, ZIP
  uploads, quota checks, and storage-hash clearing. The local runner may still
  print warnings for optional `bsdiff`/`xdelta3`/`vcdiff` tools, but the Zotero
  test case passes; separate `workerd` fixtures execute all three supported
  Worker algorithm names without those host CLIs.
- The duplicated in-memory server/stores have been removed. Local development,
  runtime tests, and compatibility work now use Wrangler/workerd with D1/R2.

## Now: prove real clients

- [ ] Decide whether this repo should ship a web UI. Zotero Desktop is a
  configurable real client; the official Zotero Web Library is not a drop-in
  self-hosted client, so a web UI would be separate product work rather than an
  official API compatibility blocker.
- [x] Reorganize `server/src` into domain, library helper, route, and type
  ownership folders without changing behavior.
- [x] Point Zotero Desktop at the deployed candidate server with a disposable
  profile and sync a small personal library.
- [x] Record the exact desktop smoke configuration: temp profile `user.js`
  points `extensions.zotero.api.url` at
  `https://zotero.peacockery.studio/`, uses Zotero storage, disables autosync,
  and installs a `/test/setup` API key through Zotero's own login manager.
- [x] Run a real-client smoke path: create book, edit book, add child note, add
  stored-file attachment, trash the note item, and confirm remote sync/version,
  R2 file download, trash, and full-text behavior.
- [x] Update `compatibility/candidate-status.md` with the client-only failures
  that the official HTTP oracle did not catch.
- [x] Add a repeatable desktop smoke command/script so the Run JavaScript
  smoke can be rerun without manual UI driving.
- [x] Make `server/` the obvious product package and collapse/delete research
  docs and local reference clones.

## Cloudflare production path

- [x] Create the real Cloudflare D1 database `zotero` and wire its
  `database_id` in `server/wrangler.jsonc`.
- [x] Create/bind the real R2 attachment bucket `zotero-attachments`.
- [x] Apply the initial D1 schema remotely and record the migration marker.
- [x] Add the Worker `nodejs_compat` flag needed for the WASM dependency path.
- [x] Run a Wrangler dry-run build to prove WASM module bundling.
- [x] Deploy the Worker as `zotero`.
- [x] Attach `zotero.peacockery.studio` as the custom domain.
- [x] Verify the deployed Worker boots with D1/R2 bindings at the Cloudflare
  edge.
- [x] Make routine deploys durable with a scoped `CLOUDFLARE_API_TOKEN` in the
  ignored local `server/.env`; no CI is required for the current workflow.
- [x] Keep `SELFHOST_TEST_API_KEY` installed as a real Cloudflare Worker secret
  so `secrets.required` can filter local Worker env inference while preserving
  one-file local Wrangler auth. Real Zotero.org API keys are separate import
  credentials, not selfhost auth keys.
- [x] Recheck plain local `curl https://zotero.peacockery.studio/health`.
- [x] Decide and implement the download URL shapes needed by the official
  `file` oracle: signed item-key URLs for normal storage and signed
  `md5/filename` URLs for old-style R2 objects.
- [x] Oracle-run the first-client board against the live D1/R2 path:
  `version,collection,item,file` is `173/173`.
- [x] Clear the remaining `file` failures with real R2/S3-compatible storage
  credentials and legacy storage URL support.
- [x] Keep D1 plus explicit version/write-token reservation; the official
  concurrent write case passes on the live D1 path.

## Official API compatibility

- [x] Drive the live official v3 suite to green for all non-pending tests:
  latest result is `451 passing`, `22 pending`, `0 failing`.
- [x] Fix concrete API gaps in `notifications` and `group`.
- [x] Implement the public `publications` endpoint behavior covered by the
  official tests.
- [x] Satisfy the full-text deindexed/reindex tests through the candidate test
  bridge and D1-backed full-text index state.
- [x] Keep the previously green API slices green while closing the remaining
  slices.
- [x] Add Cloudflare Workers Vitest coverage using real local D1/R2 bindings,
  migrations, and Worker dispatch instead of fake Hono bindings.
- [x] Pin the official Zotero oracle commit and schema digest with reproducible
  setup, status, and update commands.
- [ ] Decide whether to add local coverage for the upstream-pending `schema` and
  `tts` cases. They are pending/skipped in the official v3 run, not current
  server failures.

## Product

- [x] Confirm the public naming/trademark posture before any public launch:
  public docs use `Zotero Self-Host` and `Zotero API v3 compatible server`
  descriptively with an explicit non-affiliation disclaimer. Internal
  Cloudflare resources can keep the short `zotero` names.
- [x] Choose a license for this repo: MIT for this project's original code and
  documentation.
- [x] Clean up the Ultracite/Biome lint backlog so `bun run check` is green.
  Verified on 2026-07-09 with Ultracite, generated Wrangler types, TypeScript,
  and Vitest all passing.
- [x] Decide the auth/onboarding story for real deployments: single-owner
  self-hosting. The deployer sets root admin credentials, creates local API
  keys for their own user/library, and points compatible clients at their own
  Worker URL.
- [ ] Decide whether to add a Zotero.org import command that uses
  `ZOTERO_IMPORT_API_KEY` to copy a real library into the self-hosted server.
- [x] Add a one-click deployment template for the proven Cloudflare D1/R2 path:
  root README points Cloudflare's deploy button at the isolated `server/`
  package, with D1 migrations wired through package scripts.
