# Isolated compatibility deployment only. Never install these bindings on the
# production Worker or point this deployment at production D1/R2 resources.
DEPLOYMENT_MODE=compatibility-test
COMPATIBILITY_TEST_ADMIN_TOKEN=
COMPATIBILITY_TEST_API_KEY=
TTS_TEST_KEY=

# Disposable Zotero Desktop smoke target.
SELFHOST_URL=http://127.0.0.1:8787
