# Migrations

D1 migrations go here after the reference-stack test slice defines the first schema requirements.

Do not create Zotero sync tables before the compatibility map identifies the required version/write behavior.
